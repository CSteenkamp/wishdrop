apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: wishdrop

resources:
  - namespace.yaml
  - configmap.yaml
  - secrets.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  # - hpa.yaml          # Uncomment when metrics-server is available
  # - migration-job.yaml # Apply manually before deploys with schema changes

commonLabels:
  app.kubernetes.io/part-of: wishdrop
  app.kubernetes.io/managed-by: kustomize
